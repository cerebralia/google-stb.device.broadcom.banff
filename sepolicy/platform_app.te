allow platform_app nxserver:binder call;
allow platform_app nexus_data_file:dir { search read };
allow platform_app nexus_data_file:file { read open getattr };
allow platform_app nexus_service:service_manager find;

unix_socket_connect(platform_app, nxserver, nxserver)
